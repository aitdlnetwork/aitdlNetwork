{
  "name": "aitdl-network-workspace",
  "version": "1.0.0",
  "description": "Convenience Workspace Runner for AITDL Network",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend"
  }
}
